reg
reg<-read.csv(file=
reg<-read.csv(file='letter-sample.csv', h=T,sep=',')
summary(reg)
reg[,timestamp]
reg[timestamp]
reg[,timestamp]
reg
reg[,timestamp]
reg$timestamp
reg$timestamp[4]
reg$timestampbla="1"
bla="1"
inx=as.numeric(bla)
reg$timestamp[bla]
reg$timestamp[1]
reg$timestamp[2]
reg$timestamp[$bla]
reg$timestamp[bla]
inx=as.integer(bla)
reg$timestamp[$bla]
reg$timestamp[bla]
reg$timestamp[as.name(bla)]
bla
reg$timestamp[as.name(bla)]
reg$timestamp[as.symbol(bla)]
reg$timestamp
reg
reg$timestamp[1]
X
