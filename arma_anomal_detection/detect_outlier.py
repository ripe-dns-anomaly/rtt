def compare_values(o, p, positive, maxdif, threshold)

def detect_outlier(observations, predictions, positive=True, maxdif=None, threshold=None ):
    ''' Compares observation with prediction and return 
        index of prediction where an anomaly occured.
        Returns -1 if no anomaly was found.
        positive = True if prediction must be larger than the observation to be an outlier
        maxdif = Difference between the observation and the predction to be an outlier
        threshold = If we want to define a static threshold
     '''


    for i in range(len(observations)):
    
    


